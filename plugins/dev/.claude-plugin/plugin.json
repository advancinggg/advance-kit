{
  "name": "dev",
  "version": "3.8.0",
  "description": "Enforced dev workflow (/dev + /spec + /prd) with dual-model review, independent evaluator architecture, AC-driven MODULE progress, aligned §N.M section numbering, ADR (Architecture Decision Records) system, upstream-alignment escape hatches, worktree-parallel /dev, read-only `/dev board` snapshot dashboard, a system-acceptance witness layer, and template-version drift visibility. Includes `dev`, `spec`, `prd` skills plus opt-in statusline. Latest: 3.8.0 mechanically enforces the 3.7.0 §1.5↔§3.4 ledger parity — a new fail-open DOCS-exit PreToolUse gate (check-phase.sh + ledger-parity-check.sh) blocks a /dev run from leaving DOCS while any in-scope module's §1.5 AC is missing its §3.4 row, naming the orphan AC-IDs. Earlier: 3.7.0 §1.5↔§3.4 ledger parity (§1.5 authoritative, /dev DOCS births §3.4 rows, fail-closed SUMMARY/§6.1.1/evaluator + Ledger Parity DoD gate), 3.6.0 /dev board surfaces the system-acceptance axis (deferrals + atomic Type breakdown + per-worktree DEFER column) with durable §3 persistence, 3.5.0 /prd System-acceptance-journey marker on every §3 flow + legacy backfill.",
  "author": { "name": "Advance Studio" }
}
