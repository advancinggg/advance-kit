{
  "name": "dev",
  "version": "3.3.0",
  "description": "Enforced dev workflow (/dev + /spec + /prd) with dual-model review, independent evaluator architecture, AC-driven MODULE progress, aligned §N.M section numbering, ADR (Architecture Decision Records) system, upstream-alignment escape hatches, worktree-parallel /dev, read-only `/dev board` snapshot dashboard, a system-acceptance witness layer, and template-version drift visibility. Includes `dev`, `spec`, `prd` skills plus opt-in statusline. Latest: 3.3.0 `/spec upgrade-template` renumber-preserves on a mid-template section insert — UT.3.0 matches existing sections by title (not number), so inserting a new numbered section no longer retitle-corrupts existing docs (shifted sections keep body+title, only the number is fixed, with cascaded subheadings + §-refs; ambiguity prompts). Earlier: 3.0.0 evaluator-backed journey discovery + Phase 1.3 emergent-journey discovery, 3.1.0 atomic, independently-adjudicable SYS-AC criteria (§1.1), 3.2.0 system-acceptance harness + executability (declare/run the wired e2e + explicit user-accepted deferral).",
  "author": { "name": "Advance Studio" }
}
