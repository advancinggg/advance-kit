{
  "name": "dev",
  "version": "3.1.0",
  "description": "Enforced dev workflow (/dev + /spec + /prd) with dual-model review, independent evaluator architecture, AC-driven MODULE progress, aligned §N.M section numbering, ADR (Architecture Decision Records) system, upstream-alignment escape hatches, worktree-parallel /dev, read-only `/dev board` snapshot dashboard, a system-acceptance witness layer, and template-version drift visibility. Includes `dev`, `spec`, `prd` skills plus opt-in statusline. Latest: 3.1.0 system-acceptance journeys decompose into atomic, independently-adjudicable SYS-AC criteria (≥1 functional + NFR/SLO + error-path per journey in `docs/SYSTEM-ACCEPTANCE.md` §1.1; the status-only §2 ledger + 2-digit IDs unchanged). Earlier: 2.11.0 `/spec upgrade-template` adopts the system-acceptance layer into existing projects without a full rerun, 2.12.0 template-version drift visibility (per-skill banner + session↔installed drift + artifact `dev-template` stamps), 3.0.0 `/spec upgrade-template` evaluator-backed journey discovery + Phase 1.3 loop-until-dry emergent-journey discovery.",
  "author": { "name": "Advance Studio" }
}
