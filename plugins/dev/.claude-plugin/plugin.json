{
  "name": "dev",
  "version": "3.11.0",
  "description": "Enforced dev workflow (/dev + /spec + /prd) with dual-evaluator review (claude+codex or grok-dual backend), independent evaluator architecture, AC-driven MODULE progress, aligned §N.M section numbering, ADR (Architecture Decision Records) system, upstream-alignment escape hatches, worktree-parallel /dev, read-only `/dev board` snapshot dashboard, a system-acceptance witness layer, and template-version drift visibility. Includes `dev`, `spec`, `prd` skills plus opt-in statusline. Latest: 3.11.0 extends the Grok Build review backend to /spec and /prd — the architecture / module / UT.10 journey-discovery / PRD-coverage evaluator loops run as two parallel native `spawn_subagent` evaluators (standard auditor + hardened cross-examiner) with the same frozen positional Evaluator-A/B counters and an additive progress.json `review_backend` field; no codex CLI required anywhere under Grok Build. Earlier: 3.10.0 Grok Build review backend for /dev (runtime auto-detection, state v7 `review_backend`, every review point as two parallel spawn_subagent evaluators; claude+codex unchanged), 3.9.0 PreToolUse enforcement repair (hookSpecificOutput decision shape — the phase gate had been silently fail-open) + loop hardening, 3.8.0 mechanical fail-open DOCS-exit ledger-parity gate (check-phase.sh + ledger-parity-check.sh, orphan AC-IDs named).",
  "author": {
    "name": "Advance Studio"
  }
}
