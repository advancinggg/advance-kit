{
  "name": "dev",
  "version": "3.5.1",
  "description": "Enforced dev workflow (/dev + /spec + /prd) with dual-model review, independent evaluator architecture, AC-driven MODULE progress, aligned §N.M section numbering, ADR (Architecture Decision Records) system, upstream-alignment escape hatches, worktree-parallel /dev, read-only `/dev board` snapshot dashboard, a system-acceptance witness layer, and template-version drift visibility. Includes `dev`, `spec`, `prd` skills plus opt-in statusline. Latest: 3.5.0 /prd now requires the `System acceptance journey? Yes/No` marker on EVERY §3 flow (Phase 4 flags a missing marker) and offers a legacy backfill (batched, user-classified) so pre-2.10.0 PRDs adopt the marker without a full regenerate. Earlier: 3.2.0 system-acceptance harness + executability (declare/run the wired e2e + explicit user-accepted deferral), 3.3.0 `/spec upgrade-template` renumber-preserve on a mid-template section insert, 3.4.0 Iron-Rule honest-disclosure carve-out + structured 'Scope & unverified' field in every completion summary.",
  "author": { "name": "Advance Studio" }
}
