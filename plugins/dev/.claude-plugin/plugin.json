{
  "name": "dev",
  "version": "2.11.0",
  "description": "Enforced dev workflow (/dev + /spec + /prd) with dual-model review, independent evaluator architecture, AC-driven MODULE progress, aligned §N.M section numbering, ADR (Architecture Decision Records) system, upstream-alignment escape hatches, worktree-parallel /dev, read-only `/dev board` snapshot dashboard, and a system-acceptance witness layer. Includes `dev`, `spec`, `prd` skills plus opt-in statusline. Latest: 2.11.0 `/spec upgrade-template` adopts the system-acceptance layer into existing projects without a full rerun — injects the REQUIREMENTS_REGISTRY `Witness` column + bootstraps `docs/SYSTEM-ACCEPTANCE.md` (no evaluator loops, idempotent, merge-preserving). Earlier: 2.8.0 worktree-parallel /dev (4 `worktree-*` subcommands), 2.9.0 read-only `/dev board` snapshot dashboard, 2.10.0 system-acceptance layer (system E2E readiness axis via REQ `Witness:e2e` + `docs/SYSTEM-ACCEPTANCE.md` + /dev DoD hard gate).",
  "author": { "name": "Advance Studio" }
}
