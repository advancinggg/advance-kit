{
  "name": "dev",
  "version": "3.10.0",
  "description": "Enforced dev workflow (/dev + /spec + /prd) with dual-evaluator review (claude+codex or grok-dual backend), independent evaluator architecture, AC-driven MODULE progress, aligned §N.M section numbering, ADR (Architecture Decision Records) system, upstream-alignment escape hatches, worktree-parallel /dev, read-only `/dev board` snapshot dashboard, a system-acceptance witness layer, and template-version drift visibility. Includes `dev`, `spec`, `prd` skills plus opt-in statusline. Latest: 3.10.0 adds a Grok Build review backend — /dev auto-detects the runtime (state v7 `review_backend`) and runs every review point (plan / doc-audit / diff / test / adversarial) as two parallel native `spawn_subagent` evaluators (standard auditor + hardened cross-examiner), no codex CLI required, with frozen positional Evaluator-A/B counters; the claude+codex backend is unchanged. Earlier: 3.9.0 PreToolUse enforcement repair (hookSpecificOutput decision shape — the phase gate had been silently fail-open) + loop hardening (semantic DOCS-exit detection, auto-sync stand-down, single-execution TEST witness, per-loop round limits, arbitration logging, /spec rerun preservation, progressive disclosure), 3.8.0 mechanical fail-open DOCS-exit ledger-parity gate (check-phase.sh + ledger-parity-check.sh, orphan AC-IDs named), 3.7.0 §1.5↔§3.4 ledger parity (§1.5 authoritative, /dev DOCS births §3.4 rows, fail-closed SUMMARY/§6.1.1/evaluator + Ledger Parity DoD gate).",
  "author": {
    "name": "Advance Studio"
  }
}
