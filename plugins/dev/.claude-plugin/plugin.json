{
  "name": "dev",
  "version": "2.4.0",
  "description": "Enforced dev workflow (plan→docs→implement→audit→test) with dual-model review, independent evaluator architecture, spec-driven module decomposition, phase-gated file access control, git auto-sync with LLM-generated commit messages, `/spec upgrade-template` for section-level template migration, and new `/prd` skill for iterative PRD generation (guided dialogue + multi-dimension coverage evaluator). MODULE template (2.3.0+) adds §1.1 Serves PRD topics reverse mapping, §2.13 Operations (runbook), §2.14 Observability. Adds /dev PLAN routing via CONTEXT-MAP + domain-term resolution via GLOSSARY (auto-generated by /spec and /prd).",
  "author": { "name": "Advance Studio" }
}
