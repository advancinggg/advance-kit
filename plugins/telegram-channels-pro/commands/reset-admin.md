---
description: Reset the telegram-channels-pro admin allowlist + force-reopen registration window (in-process; no daemon restart)
---

Run the reset-admin helper:

!`bash "${CLAUDE_PLUGIN_ROOT:-$(pwd)/plugins/telegram-channels-pro}/bin/reset-admin-helper.sh"`
