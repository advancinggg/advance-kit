---
description: Install the telegram-channels-pro daemon under launchd (or opt out for lazy-spawn fallback)
---

Run the install helper:

!`bash "${CLAUDE_PLUGIN_ROOT:-$(pwd)/plugins/telegram-channels-pro}/bin/launchctl-helper.sh" install`
